#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	cout << "введи a, b, c  переменные" << endl;
	int a,b,c;
	cin >> a >> b >> c;
	float r = 2*a+4*b/pow(static_cast<float>(c),2);
	cout << "2a+4b/c^2 = " << r;
}
